import{_ as e,c as s,o as n,e as a}from"./app-0TPXc-ei.js";const c={},p=a(`<h1 id="css-单位指南-css-em、rem、vh、vw-等详解" tabindex="-1"><a class="header-anchor" href="#css-单位指南-css-em、rem、vh、vw-等详解"><span>CSS 单位指南：CSS em、rem、vh、vw 等详解</span></a></h1><p>许多 CSS 属性如<code>width</code>、<code>margin</code>、<code>padding</code>和<code>font-size</code>都需要一个长度，而 CSS 有许多不同的方法来表达长度。</p><p>在 CSS 中，长度是一个数字，一个没有空格的单位。例如，<code>5px</code>、<code>0.9em</code>，等等。</p><p>在 CSS 中，一般有两种用于长度和尺寸的单位：绝对单位和相对单位。</p><h2 id="绝对长度单位" tabindex="-1"><a class="header-anchor" href="#绝对长度单位"><span>绝对长度单位</span></a></h2><p>绝对长度单位是基于一个实际的物理单位，通常被认为是跨设备的相同尺寸。但是，根据你的屏幕尺寸和质量，或者你的浏览器或操作系统的设置，可能会有一些例外。</p><p>下面是 CSS 中一些常见的绝对长度单位：</p><p><code>px</code></p><p>像素，或<code>px</code>，是 CSS 中最常见的长度单位之一。</p><p>在 CSS 中，1 像素被<a href="https://drafts.csswg.org/css-values/#reference-pixel" target="_blank" rel="noopener noreferrer">正式定义</a>为 1/96 英寸。所有其他的绝对长度单位都是基于这个像素的定义。</p><p>但是，在最初制定这一标准时，大多数显示器的分辨率为 1024×768，DPI（每英寸点数）为 96。</p><p>现代设备的屏幕具有更高的分辨率和 DPI，所以 96 像素长的线可能无法精确测量到 1 英寸，这取决于设备的情况。</p><p>尽管以像素为单位的尺寸在不同的设备上会有所不同，但一般认为在屏幕上使用像素更好。</p><p>如果你知道你的页面将在高质量的打印机上打印，那么你可以考虑使用另一个单位，比如<code>cm</code>或<code>mm</code>。</p><p>你可以<a href="https://www.smashingmagazine.com/2021/07/css-absolute-units/" target="_blank" rel="noopener noreferrer">在这篇文章</a>中了解更多关于像素单位的历史，以及为什么 CSS 英寸并不总是与物理英寸相匹配。</p><p><code>cm</code></p><p>厘米</p><p>在 CSS 中，<code>1cm</code>大约是 37.8 个像素，或约为 25.2/64 英寸。</p><p><code>mm</code></p><p>毫米</p><p>在 CSS 中，<code>1mm</code>大约是 3.78 像素，或 1/10 厘米。</p><p><code>in</code></p><p>英寸</p><p>在 CSS 中，<code>1in</code>大约是 96 个像素，或大约 2.54 厘米。</p><p><code>pt</code></p><p>磅</p><p>在 CSS 中，<code>1pt</code>大约是 1.3333 像素，或 1/72 英寸。</p><p><code>pc</code></p><p>派卡</p><p>在 CSS 中，<code>1pc</code>大约是 16 个像素，或 1/6 英寸。</p><h2 id="相对长度单位" tabindex="-1"><a class="header-anchor" href="#相对长度单位"><span>相对长度单位</span></a></h2><p>相对长度单位是相对于另一个元素的大小或设置。例如，一个元素的相对字体大小可以用父元素的字体大小来计算。</p><p>下面是一些常见的相对长度单位：</p><p><code>em</code></p><p>CSS 的<code>em</code>单位的名字来自于一个排版单位。在字体排印学中，em 这个词“<a href="https://www.wikiwand.com/zh/Em_(%E5%AD%97%E4%BD%93%E6%8E%92%E5%8D%B0%E5%AD%A6)" target="_blank" rel="noopener noreferrer">最初是指所使用的字体和尺寸中大写字母 M 的宽度</a>”。</p><p>当与<code>font-size</code>属性一起使用时，<code>em</code>继承其父元素的<code>font-size</code>大小：</p><div class="language-css line-numbers-mode" data-highlighter="prismjs" data-ext="css" data-title="css"><pre class="language-css"><code><span class="line"><span class="token selector">.container</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">font-size</span><span class="token punctuation">:</span> 16px<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token selector">.container p</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">font-size</span><span class="token punctuation">:</span> 1em<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token selector">.container h2</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">font-size</span><span class="token punctuation">:</span> 3em<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token selector">.container h3</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">font-size</span><span class="token punctuation">:</span> 2em<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，<code>p</code>的<code>font-size</code>是<code>16px</code>（16 × 1）。同时，<code>h2</code>的<code>font-size</code>为<code>48px</code>（16 × 3），<code>h3</code>为<code>32px</code>（16 × 2）。</p><p>如果<code>em</code>与另一个属性（如<code>width</code>）一起使用，<code>em</code>是用目标元素的大小来计算的。</p><p><code>rem</code></p><p>根<code>em</code>。这种相对单位不受父元素的大小或设置的影响，而是以文档的根为基础。对于网站来说，文档的根是<code>html</code>元素。</p><div class="language-css line-numbers-mode" data-highlighter="prismjs" data-ext="css" data-title="css"><pre class="language-css"><code><span class="line"><span class="token selector">p</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">font-size</span><span class="token punctuation">:</span> 1.25rem<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在大多数浏览器中，默认的字体大小是 16，所以<code>html</code>元素的<code>font-size</code>是<code>16px</code>。所以在这种情况下，<code>p</code>是<code>20px</code>（16 × 1.25）。</p><p>但是如果用户改变了他们浏览器的默认字体大小，那么<code>p</code>的<code>font-size</code>就会相应地放大或缩小。</p><p><code>%</code></p><p>百分比，或相对于父级大小的百分比大小：</p><div class="language-css line-numbers-mode" data-highlighter="prismjs" data-ext="css" data-title="css"><pre class="language-css"><code><span class="line"><span class="token selector">div</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">width</span><span class="token punctuation">:</span> 400px<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line"><span class="token selector">div p</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">width</span><span class="token punctuation">:</span> 75%<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于父级的宽度是<code>400px</code>，所以内部段落的宽度是<code>300px</code>（400 × 0.75）。</p><p><code>vw</code></p><p>视图宽度，<code>1vw</code>是视口宽度的 1%。</p><div class="language-css line-numbers-mode" data-highlighter="prismjs" data-ext="css" data-title="css"><pre class="language-css"><code><span class="line"><span class="token selector">body</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">width</span><span class="token punctuation">:</span> 100vw<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于<code>body</code>元素被设置为<code>100vw</code>，即视口宽度的 100%，所以它将占用它所能获得的全部宽度。因此，如果你把浏览器的大小调整为 690 像素宽，那么<code>body</code>就会占据所有 690 像素的宽度。</p><p><code>vh</code></p><p>视图高度，<code>1vh</code>是视口高度的 1%。</p><div class="language-css line-numbers-mode" data-highlighter="prismjs" data-ext="css" data-title="css"><pre class="language-css"><code><span class="line"><span class="token selector">div</span> <span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">height</span><span class="token punctuation">:</span> 50vh<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>该<code>div</code>将填充视口高度的 50%。因此，如果浏览器窗口的高度是 900 像素，那么该<code>div</code>的高度将是 450 像素。</p><p><code>ex</code></p><p>CSS<code>ex</code>单位的名称来自于字体排印学中的 x-字高，即“<a href="https://www.wikiwand.com/zh/X%E5%AD%97%E9%AB%98" target="_blank" rel="noopener noreferrer">字体中字母 x 的高度</a>”。在许多字体中，小写的 x 字符通常是最大字符高度的一半。</p><p>在 CSS 中，<code>1ex</code>是字体的 x-字高，或<code>1em</code>的一半。</p><p>但是，由于小写字母 x 的大小可以根据字体的不同而有很大的变化，因此 CSS 的<code>ex</code>单位很少被使用。</p><p><code>ch</code></p><p>字符单位，CSS 的<code>ch</code>单位被定义为字体的 0（零，或U+0030）字符的宽度。</p><p>虽然<code>ch</code>单位对于像 Courier 这样的单行线/固定宽度的字体来说是一种精确的测量，但对于像 Arial 这样的比例字体来说，它可能是不可预测的。</p><p>例如，如果你的字体是 Courier，而你把一个元素的宽度设置为<code>60ch</code>，那么这个元素的宽度就正好是 60 个字符。</p><p>但是，如果你的字体是 Arial，而你把一个元素的宽度设置为<code>60ch</code>，那么就不知道这个元素会有多宽——字符可能会溢出容器，也可能不够。</p><p>请看<a href="https://meyerweb.com/eric/thoughts/2018/06/28/what-is-the-css-ch-unit/" target="_blank" rel="noopener noreferrer">这篇文章</a>，了解对<code>ch</code>单位的深入解释，并看一些例子。</p><p><code>vmin</code>和<code>vmax</code></p><p>视口最小（<code>vmin</code>）和视口最大（<code>vmax</code>）的单位是基于<code>vw</code>和<code>vh</code>的值。</p><p><code>1vmin</code>是视口最小尺寸的 1%，而<code>1vmax</code>是视口最大尺寸的 1%。</p><p>例如，想象一个宽 1200 像素、高 600 像素的浏览器窗口。在这种情况下，<code>1vmin</code>是<code>6px</code>（<code>vh</code>的 1%，即 600 像素时较小值）。同时，<code>1vmax</code>是<code>12px</code>（<code>vh</code> 的 1%，即 1200 像素时的较大值）。</p><h2 id="相关链接" tabindex="-1"><a class="header-anchor" href="#相关链接"><span>相关链接</span></a></h2><p><a href="https://www.freecodecamp.org/chinese/news/css-unit-guide/" title="CSS 单位指南：CSS em、rem、vh、vw 等详解" target="_blank" rel="noopener noreferrer">CSS 单位指南：CSS em、rem、vh、vw 等详解</a></p>`,72),o=[p];function d(i,l){return n(),s("div",null,o)}const r=e(c,[["render",d],["__file","css-unit-guide.html.vue"]]),u=JSON.parse('{"path":"/archive/css-unit-guide.html","title":"CSS 单位指南：CSS em、rem、vh、vw 等详解","lang":"zh-CN","frontmatter":{},"headers":[{"level":2,"title":"绝对长度单位","slug":"绝对长度单位","link":"#绝对长度单位","children":[]},{"level":2,"title":"相对长度单位","slug":"相对长度单位","link":"#相对长度单位","children":[]},{"level":2,"title":"相关链接","slug":"相关链接","link":"#相关链接","children":[]}],"git":{},"filePathRelative":"archive/css-unit-guide.md"}');export{r as comp,u as data};
